# rift project configuration file

name = "wallet"

[contracts.wallet]
    contract = "Wallet" # This is the contract class you have in the file
    deploy = "deploy_wallet" # This is name of the deploy script
    # All test scripts should be mentioned here
    tests = [
        "test_wallet"
    ]
